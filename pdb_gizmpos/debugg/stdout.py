# module that contains function that is responsible for printing out data in a terminal
import os

# pdbTools inports
from pdb_gizmpos.utils.finder import Finder

def search_trajectories(path='.'):
    """ rhow all the trajectories that are presents in the given path """

    pass

def search_pdbfiles(path="."):
    """ returns a list of all pdbfiles located in the specified directory. [default="."]"""
    pass